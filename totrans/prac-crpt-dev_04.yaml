- en: Hash Functions
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: 原文：[https://cryptobook.nakov.com/cryptographic-hash-functions](https://cryptobook.nakov.com/cryptographic-hash-functions)
  prefs:
  - PREF_BQ
  type: TYPE_NORMAL
- en: In computer programming **hash functions** map text (or other data) to integer
    numbers. Usually different inputs maps to different outputs, but sometimes a **collision**
    may happen (different input with the same output).
  prefs: []
  type: TYPE_NORMAL
- en: '**Cryptographic hash** functions transform text or binary data to fixed-length
    **hash value** and are known to be **collision-resistant** and **irreversible**.
    Example of cryptographic hash function is **SHA3-256**:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE0]'
  prefs: []
  type: TYPE_PRE
- en: 'The above SHA3-256 hash calculation can be coded in Python like this:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE1]'
  prefs: []
  type: TYPE_PRE
- en: 'Run the above code example: [https://repl.it/@nakov/SHA3-256-hello-in-Python](https://repl.it/@nakov/SHA3-256-hello-in-Python).'
  prefs: []
  type: TYPE_NORMAL
- en: 'The same SHA3-256 hash calculation can be coded in JavaScript like this (after
    `npm install js-sha3`):'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE2]'
  prefs: []
  type: TYPE_PRE
- en: 'Run the above code example: [https://repl.it/@nakov/SHA3-256-hello-in-JS](https://repl.it/@nakov/SHA3-256-hello-in-JS).'
  prefs: []
  type: TYPE_NORMAL
- en: '[](#hashing-in-software-engineering)Hashing (in Software Engineering)'
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: The process of calculating the value of certain hash function is called "**hashing**".
  prefs: []
  type: TYPE_NORMAL
- en: '![](../Images/8cf47b9b5d9d0a6277687781472eb6aa.png)'
  prefs: []
  type: TYPE_IMG
- en: In the above example the text `John Smith` is hashed to the hash value `02`
    and `Lisa Smith` is hashed to `01`. The input texts `John Smith` and `Sandra Dee`
    both are hashed to `02` and this is called "**collision**".
  prefs: []
  type: TYPE_NORMAL
- en: Hash functions are **irreversible by design**, which means that there is no
    fast algorithm to restore the input message from its hash value.
  prefs: []
  type: TYPE_NORMAL
- en: In programming **hash functions** are used in the implementation of the data
    structure "**hash-table**" (associative array) which maps values of certain input
    type to values of another type, e.g. map product name (text) to product price
    (decimal number).
  prefs: []
  type: TYPE_NORMAL
- en: A **naive hash function** is just to sum the bytes of the input data / text.
    It causes a lot of collisions, e.g. `hello` and `ehllo` will have the same hash
    code. **Better hash functions** may use the [Merkle–Damgård construction](https://en.wikipedia.org/wiki/Merkle–Damgård_construction)
    scheme, which takes the first byte as **state**, then **transforms the state**
    (e.g. multiplies it by a prime number like 31), then **adds the next byte** to
    the state, then again transforms the state and adds the next byte, etc. This significantly
    reduces the rate of collisions and produces better distribution.
  prefs: []
  type: TYPE_NORMAL
- en: '[](#cryptographic-hash-functions)Cryptographic Hash Functions'
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: In cryptography, **hash functions** transform **input data** of arbitrary size
    (e.g. a text message) to a **result** of fixed size (e.g. 256 bits), which is
    called **hash value** (or hash code, message digest, or simply hash). Hash functions
    (hashing algorithms) used in computer cryptography are known as "**cryptographic
    hash functions**". Examples of such functions are **SHA-256** and **SHA3-256**,
    which transform arbitrary input to 256-bit output.
  prefs: []
  type: TYPE_NORMAL
- en: '![](../Images/6eb31c73fc88125f518ca407438abcd9.png)'
  prefs: []
  type: TYPE_IMG
- en: '[](#cryptographic-hash-functions-examples)Cryptographic Hash Functions - Examples'
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: 'As an **example**, we can take the cryptographic hash function `SHA-256` and
    calculate the hash value of certain text message `hello`:'
  prefs: []
  type: TYPE_NORMAL
- en: <template id="B:2"></template>
  prefs: []
  type: TYPE_NORMAL
